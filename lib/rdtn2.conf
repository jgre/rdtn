# -*-ruby-*-

loglevel :debug
log :info,"dtnd parsing configuration..."

interface :add, :tcp, "tcp0", "-p 7000 -h localhost"
#interface :add, :client, "client0"

link :add, "tcplink1", "localhost:5555", :alwayson, :tcp
